Что уже хорошо ✅

Зона	Почему ок
Чёткая иерархия данных (Course → Module → Lesson)	Простое расширение (можно вложить «sub-lesson» или «quiz»)
Enum-поля level / type / difficulty	Предсказуемые фильтры и цветовая кодировка
AI-ассистент как вкладка, не pop-up	Пользователь не теряет контекст урока
Связи с Roadmap / LabHub	Прогресс синхронизируется, нет дублирования логики
React Query + local persist	Работает оффлайн и плавно «доотправляет» прогресс
Что подправить или расширить 🛠️

Тема	Замечание	Как улучшить
1. Гибкость уровней курсов	`level: 'basic'	'practice'
2. Версионирование контента	Сейчас нет поля для версии курса/урока. При обновлении материала ломается прогресс.	Добавить version: string в Course и хранить history; если версия ↑ — предлагать «обновить и пересдать».
3. Масштабируется ли Module?	Уроки напрямую внутри Module ⇒ сложно делать большие ветки.	Ввести суб-модели: Section или Unit, чтобы не было списков по 50 уроков.
4. Пререквизиты	prerequisites: string[] в Lesson — норм, но лучше хранить не id уроков, а “skill tags”.	Добавить таблицу Skill { id, name } и lessonSkills (many-to-many).
5. Access Control для PRO	Нет поля «доступ по подписке».	В Course: `access: 'free'
6. Quiz / интерактив	В Lesson type = 'quiz', но структуру вопросов не описали.	Модель Quiz { id, questions: Question[] }, Question { prompt, answers[], correctIds[] }.
7. Ассистенты	Переключение между ассистентами — хорошо, но нет fallback если API-лимит.	Ввести лимит запросов/день + cache наиболее частых ответов по уроку.
8. SEO / deeplink	У курсов нет slug для человекопонятного URL.	slug: string в Course; роут …/courses/<slug>.
9. Безопасность данных	Локальный прогресс ≠ шифрован.	Шифровать IndexedDB/Apollo cache (crypto-js) или ограничиваться non-PII.
10. A/B-эксперименты	Нет схемы для тестирования вариантов урока.	LessonVariant { id, lessonId, variant, isDefault } + feature-flag в React Query.
UX-мелочи для «вау» ✨
Quick view: наведи на карточку курса → всплывает мини-превью (модули, рейтинг, CTA).

Adaptive ETA: в модуле показывать «Осталось ~37 мин» на основе усреднённого времени пользователя.

Progress ring animations: использовать framer-motion spring на +10 % — dopamine hit.

Keyboard shortcuts: J/K — следующий / предыдущий урок; ? — подсказка.

“Resume last viewed” плашка на вершине каталога.

